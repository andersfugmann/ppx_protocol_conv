Handle more types:
  * adt's
  * recursive types (and mutial recursive types)
  * polymorphic variants

Add driver for msgpack
Split into multiple packages, so we dont require all to install
   yojson, xml or msgpack
