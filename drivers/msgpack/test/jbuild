(jbuild_version 1)

(executable
 ((name       unittest)
  (modules    (unittest))
  (libraries  (test protocol_conv_msgpack))
))

(alias
 ((name   runtest)
  (package ppx_protocol_conv_msgpack)
  (action (run ${exe:unittest.exe}))
))
