(jbuild_version 1)

(executable
 ((name       unittest)
  (modules    (unittest))
  (libraries  (test ppx_protocol_conv_yaml))
))

(alias
 ((name   runtest)
  (package ppx_protocol_conv)
  (action (run ${exe:unittest.exe}))
))
